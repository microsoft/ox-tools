# Copyright (c) Microsoft Corporation.
# Licensed under the MIT License.
# GENERATED BY cargo-anvil. DO NOT EDIT DIRECTLY.
# Update cargo-anvil and regenerate; repository-specific edits stop automatic updates.
# Update behaviour: https://github.com/microsoft/ox-tools/blob/main/crates/cargo-anvil/docs/design/updates.md
#
# Restores the benchmark history the previous scheduled run published.
#
# See https://github.com/microsoft/ox-tools/blob/main/crates/cargo-anvil/docs/design/benchmarks.md
parameters:
  - name: artifact
    type: string
  - name: path
    type: string
    default: target/anvil/bench-history
steps:
  # The store has to exist even when there is nothing to restore, so the
  # first ever run analyzes an empty history instead of erroring.
  - pwsh: New-Item -ItemType Directory -Force -Path '${{ parameters.path }}' | Out-Null
    displayName: Prepare benchmark history store
  - task: DownloadPipelineArtifact@2
    displayName: Restore ${{ parameters.artifact }}
    # The first run has no artifact to restore; a missing artifact is a
    # cold start, not a failure.
    continueOnError: true
    inputs:
      buildType: specific
      project: $(System.TeamProjectId)
      definition: $(System.DefinitionId)
      buildVersionToDownload: latestFromBranch
      branchName: $(Build.SourceBranch)
      # Take the newest run that carries the artifact whatever its
      # outcome. Restoring only from green runs would drop every sample
      # collected while the pipeline was red from a regression --
      # precisely the window that matters.
      allowPartiallySucceededBuilds: true
      allowFailedBuilds: true
      artifactName: ${{ parameters.artifact }}
      targetPath: ${{ parameters.path }}
