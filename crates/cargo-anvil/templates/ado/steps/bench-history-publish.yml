# Copyright (c) Microsoft Corporation.
# Licensed under the MIT License.
# GENERATED BY cargo-anvil. DO NOT EDIT DIRECTLY.
# Update cargo-anvil and regenerate; repository-specific edits stop automatic updates.
# Update behaviour: https://github.com/microsoft/ox-tools/blob/main/crates/cargo-anvil/docs/design/updates.md
#
# Publishes the updated benchmark history as this leg's artifact.
#
# This does not go through the §4.1 job wrapper's `artifacts` contract
# because it needs a condition the contract does not express: the store is
# only a valid continuation of the chain when the restore reached a known
# state. Publishing after an operational restore failure would overwrite a
# good history with a truncated snapshot.
#
# The condition still includes failed runs: a flagged regression fails the
# job, and those samples belong in the history.
#
# See https://github.com/microsoft/ox-tools/blob/main/crates/cargo-anvil/docs/design/benchmarks.md
parameters:
  - name: artifact
    type: string
  - name: path
    type: string
    default: target/anvil/bench-history
steps:
  - task: PublishPipelineArtifact@1
    displayName: Publish ${{ parameters.artifact }}
    condition: and(succeededOrFailed(), ne(variables['ANVIL_BENCH_RESTORE'], ''))
    inputs:
      targetPath: ${{ parameters.path }}
      artifact: ${{ parameters.artifact }}
