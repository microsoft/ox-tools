# Copyright (c) Microsoft Corporation.
# Licensed under the MIT License.
# Managed by cargo-anvil. Update the corresponding template in the cargo-anvil
# crate unless the change is repository-specific.
# Update behaviour: https://github.com/microsoft/ox-tools/blob/main/crates/cargo-anvil/docs/design/updates.md
trigger: none

stages:
  - template: anvil/pr.yml
    parameters:
      linuxPool: { vmImage: ubuntu-latest }
      windowsPool: { vmImage: windows-latest }
  # Repo-owned extension point: add your own PR-tier stages in
  # anvil/custom-pr-stages.yml (emitted as an empty stub) without editing
  # this anvil-owned root pipeline.
  - template: anvil/custom-pr-stages.yml
    parameters:
      linuxPool: { vmImage: ubuntu-latest }
      windowsPool: { vmImage: windows-latest }
