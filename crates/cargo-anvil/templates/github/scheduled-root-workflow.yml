# Copyright (c) Microsoft Corporation.
# Licensed under the MIT License.
# Managed by cargo-anvil. Update the corresponding template in the cargo-anvil
# crate unless the change is repository-specific.
# Update behaviour: https://github.com/microsoft/ox-tools/blob/main/crates/cargo-anvil/docs/design/updates.md
name: anvil-scheduled

on:
  schedule:
    - cron: "0 7 * * *"
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  anvil-scheduled:
    uses: ./.github/workflows/anvil-scheduled-impl.yml
    permissions:
      contents: read
      # Restoring the history reads the Actions runs/artifacts API.
      actions: read
      # The scheduled-benchmarks job files the regression tracking issue.
      # A reusable workflow cannot grant itself more than the caller does,
      # so the grant is repeated here and narrowed to that one job inside.
      issues: write
    secrets: inherit
